tool_id: gowitness
version: 1.0.0
phase: recon
category: recon
risk_level: passive
requires_approval: false
default_timeout_s: 300
description: gowitness headless-Chrome screenshots from a URL list. Backlog/dev1_md
  §4.4.
command_template:
- gowitness
- file
- -f
- '{in_dir}/urls.txt'
- --destination
- '{out_dir}/screens'
command_string: gowitness file -f {in_dir}/urls.txt --destination {out_dir}/screens
placeholders:
  in_dir}/urls.txt: <in_dir}/urls.txt>
  out_dir}/screens: <out_dir}/screens>
output_format: json_object
evidence_artifacts: &id001
- /out/screens
cwe_ids: []
owasp_wstg:
- WSTG-INFO-02
- WSTG-INFO-08
image: argus-kali-web:latest
network_policy_name: recon-passive
scenarios:
- description: Run gowitness against target
  example: gowitness file -f {in_dir}/urls.txt --destination {out_dir}/screens
  output_type: json_object
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: Information gathering and discovery phase. gowitness headless-Chrome
  screenshots from a URL list. Backlog/dev1_md §4.4.
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
