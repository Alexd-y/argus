tool_id: enum4linux_ng
version: 1.0.0
phase: recon
category: network
risk_level: low
requires_approval: false
default_timeout_s: 1800
description: enum4linux-ng full SMB/NetBIOS enumeration including users, groups, shares,
  and policies. Backlog/dev1_md §4.2.
command_template:
- enum4linux-ng
- -A
- -oJ
- '{out_dir}/enum4linux.json'
- '{ip}'
command_string: enum4linux-ng -A -oJ {out_dir}/enum4linux.json {ip}
placeholders:
  out_dir}/enum4linux.json: <out_dir}/enum4linux.json>
  ip: 10.10.1.100
output_format: json_object
evidence_artifacts: &id001
- /out/enum4linux.json
cwe_ids:
- 200
- 285
owasp_wstg:
- WSTG-INFO-04
image: argus-kali-recon:latest
network_policy_name: recon-smb
scenarios:
- description: Run enum4linux_ng against target
  example: enum4linux-ng -A -oJ {out_dir}/enum4linux.json {ip}
  output_type: json_object
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: Information gathering and discovery phase. enum4linux-ng full SMB/NetBIOS
  enumeration including users, groups, shares, and policies. Backlog/dev1_md §4.2.
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
