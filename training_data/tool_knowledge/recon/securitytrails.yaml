tool_id: securitytrails
version: 1.0.0
phase: recon
category: recon
risk_level: passive
requires_approval: false
default_timeout_s: 300
description: SecurityTrails passive subdomain lookup via the v1 REST API. Requires
  SECURITYTRAILS_API_KEY env. Backlog/dev1_md §4.1.
command_template:
- sh
- -c
- 'curl -sS --fail-with-body --max-time 60 -H "APIKEY: ${SECURITYTRAILS_API_KEY:?missing}"
  ''https://api.securitytrails.com/v1/domain/{domain}/subdomains'' -o {out_dir}/securitytrails.json'
command_string: 'sh -c curl -sS --fail-with-body --max-time 60 -H "APIKEY: ${SECURITYTRAILS_API_KEY:?missing}"
  ''https://api.securitytrails.com/v1/domain/{domain}/subdomains'' -o {out_dir}/securitytrails.json'
placeholders:
  ? 'curl -sS --fail-with-body --max-time 60 -H "APIKEY: ${SECURITYTRAILS_API_KEY:?missing}"
    ''https://api.securitytrails.com/v1/domain/{domain}/subdomains'' -o {out_dir}/securitytrails.json'
  : '<curl -sS --fail-with-body --max-time 60 -H "APIKEY: ${SECURITYTRAILS_API_KEY:?missing}"
    ''https://api.securitytrails.com/v1/domain/{domain}/subdomains'' -o {out_dir}/securitytrails.json>'
output_format: custom
evidence_artifacts: &id001
- /out/securitytrails.json
cwe_ids: []
owasp_wstg:
- WSTG-INFO-01
image: argus-kali-recon:latest
network_policy_name: recon-passive
scenarios:
- description: Run securitytrails against target
  example: 'sh -c curl -sS --fail-with-body --max-time 60 -H "APIKEY: ${SECURITYTRAILS_API_KEY:?missing}"
    ''https://api.securitytrails.com/v1/domain/{domain}/subdomains'' -o {out_dir}/securitytrails.json'
  output_type: custom
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: Information gathering and discovery phase. SecurityTrails passive subdomain
  lookup via the v1 REST API. Requires SECURITYTRAILS_API_KEY env. Backlog/dev1_md
  §4.1.
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
