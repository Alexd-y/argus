tool_id: otx_alienvault
version: 1.0.0
phase: recon
category: recon
risk_level: passive
requires_approval: false
default_timeout_s: 300
description: AlienVault OTX passive DNS lookup for historical resolutions of the target
  domain. Backlog/dev1_md §4.1.
command_template:
- sh
- -c
- curl -sS --fail-with-body --max-time 60 'https://otx.alienvault.com/api/v1/indicators/domain/{domain}/passive_dns'
  -o {out_dir}/otx.json
command_string: sh -c curl -sS --fail-with-body --max-time 60 'https://otx.alienvault.com/api/v1/indicators/domain/{domain}/passive_dns'
  -o {out_dir}/otx.json
placeholders:
  ? curl -sS --fail-with-body --max-time 60 'https://otx.alienvault.com/api/v1/indicators/domain/{domain}/passive_dns'
    -o {out_dir}/otx.json
  : <curl -sS --fail-with-body --max-time 60 'https://otx.alienvault.com/api/v1/indicators/domain/{domain}/passive_dns'
    -o {out_dir}/otx.json>
output_format: custom
evidence_artifacts: &id001
- /out/otx.json
cwe_ids: []
owasp_wstg:
- WSTG-INFO-01
image: argus-kali-recon:latest
network_policy_name: recon-passive
scenarios:
- description: Run otx_alienvault against target
  example: sh -c curl -sS --fail-with-body --max-time 60 'https://otx.alienvault.com/api/v1/indicators/domain/{domain}/passive_dns'
    -o {out_dir}/otx.json
  output_type: custom
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: Information gathering and discovery phase. AlienVault OTX passive DNS
  lookup for historical resolutions of the target domain. Backlog/dev1_md §4.1.
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
