tool_id: ldapsearch
version: 1.0.0
phase: recon
category: network
risk_level: low
requires_approval: false
default_timeout_s: 600
description: OpenLDAP ldapsearch — anonymous LDAP enumeration (-x simple bind) against
  {host} below {basedn}. -s sub recurses the full subtree. Read-only operation, but
  emits operator-level information disclosure on misconfigured directories that allow
  anonymous binds. Output text-only (parser deferred to Cycle 3). Backlog/dev1_md
  §4.17. ARG-019.
command_template:
- sh
- -c
- ldapsearch -x -H ldap://{host} -b {basedn} -s sub > {out_dir}/ldapsearch.txt
command_string: sh -c ldapsearch -x -H ldap://{host} -b {basedn} -s sub > {out_dir}/ldapsearch.txt
placeholders:
  ldapsearch -x -H ldap://{host} -b {basedn} -s sub > {out_dir}/ldapsearch.txt: <ldapsearch
    -x -H ldap://{host} -b {basedn} -s sub > {out_dir}/ldapsearch.txt>
output_format: text_lines
evidence_artifacts: &id001
- /out/ldapsearch.txt
cwe_ids:
- 200
- 287
owasp_wstg:
- WSTG-INFO-05
- WSTG-ATHN-04
image: argus-kali-network:latest
network_policy_name: auth-bruteforce
scenarios:
- description: Run ldapsearch against target
  example: sh -c ldapsearch -x -H ldap://{host} -b {basedn} -s sub > {out_dir}/ldapsearch.txt
  output_type: text_lines
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: 'Information gathering and discovery phase. OpenLDAP ldapsearch — anonymous
  LDAP enumeration (-x simple bind) against {host} below {basedn}. -s sub recurses
  the full subtree. Read-only operation, but emits operator-level information disclosure '
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
