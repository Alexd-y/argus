tool_id: jarm
version: 1.0.0
phase: recon
category: recon
risk_level: passive
requires_approval: false
default_timeout_s: 120
description: JARM TLS server fingerprinting from a URL list. Backlog/dev1_md §4.4.
command_template:
- jarm
- -i
- '{in_dir}/urls.txt'
- -j
- '{out_dir}/jarm.json'
command_string: jarm -i {in_dir}/urls.txt -j {out_dir}/jarm.json
placeholders:
  in_dir}/urls.txt: <in_dir}/urls.txt>
  out_dir}/jarm.json: <out_dir}/jarm.json>
output_format: json_object
evidence_artifacts: &id001
- /out/jarm.json
cwe_ids: []
owasp_wstg:
- WSTG-INFO-02
- WSTG-INFO-08
image: argus-kali-web:latest
network_policy_name: recon-passive
scenarios:
- description: Run jarm against target
  example: jarm -i {in_dir}/urls.txt -j {out_dir}/jarm.json
  output_type: json_object
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: Information gathering and discovery phase. JARM TLS server fingerprinting
  from a URL list. Backlog/dev1_md §4.4.
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
