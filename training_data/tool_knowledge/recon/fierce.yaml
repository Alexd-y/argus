tool_id: fierce
version: 1.0.0
phase: recon
category: recon
risk_level: passive
requires_approval: false
default_timeout_s: 1200
description: fierce DNS recon walking adjacent IPs and brute-forcing hostnames within
  the target domain. Backlog/dev1_md §4.1.
command_template:
- fierce
- --domain
- '{domain}'
- --json
- '{out_dir}/fierce.json'
command_string: fierce --domain {domain} --json {out_dir}/fierce.json
placeholders:
  domain: <domain>
  out_dir}/fierce.json: <out_dir}/fierce.json>
output_format: json_object
evidence_artifacts: &id001
- /out/fierce.json
cwe_ids: []
owasp_wstg:
- WSTG-INFO-01
- WSTG-INFO-02
image: argus-kali-recon:latest
network_policy_name: recon-passive
scenarios:
- description: Run fierce against target
  example: fierce --domain {domain} --json {out_dir}/fierce.json
  output_type: json_object
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: Information gathering and discovery phase. fierce DNS recon walking adjacent
  IPs and brute-forcing hostnames within the target domain. Backlog/dev1_md §4.1.
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
