tool_id: webanalyze
version: 1.0.0
phase: recon
category: recon
risk_level: passive
requires_approval: false
default_timeout_s: 120
description: webanalyze (Wappalyzer-port) tech-stack scan (stdout JSON; image wrapper
  persists to /out/webanalyze.json). Backlog/dev1_md §4.4.
command_template:
- webanalyze
- -host
- '{host}'
- -output
- json
command_string: webanalyze -host {host} -output json
placeholders:
  host: target.example.com
output_format: json_object
evidence_artifacts: &id001
- /out/webanalyze.json
cwe_ids: []
owasp_wstg:
- WSTG-INFO-02
- WSTG-INFO-08
image: argus-kali-web:latest
network_policy_name: recon-passive
scenarios:
- description: Run webanalyze against target
  example: webanalyze -host {host} -output json
  output_type: json_object
  next_steps:
  - Follow up with related ARGUS tools based on findings
related_tools: []
when_to_use: Information gathering and discovery phase. webanalyze (Wappalyzer-port)
  tech-stack scan (stdout JSON; image wrapper persists to /out/webanalyze.json). Backlog/dev1_md
  §4.4.
key_outputs: *id001
tips:
- Start with default flags, then add aggressive options for thorough scanning
