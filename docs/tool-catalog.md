# ARGUS sandbox tool catalog

<!-- AUTO-GENERATED by `python -m scripts.docs_tool_catalog --out ../docs/tool-catalog.md` -->
<!-- DO NOT EDIT BY HAND. Re-run the script after changing any YAML in -->
<!-- `backend/config/tools/`. The script verifies every signature and -->
<!-- renders this file deterministically (stable order: phase → tool_id). -->

This document indexes the **157** tool descriptors that the ARGUS Active Pentest Engine ships through cycles **ARG-001..ARG-049** (Backlog/dev1_md §4.1–§4.19). Each entry is loaded via `src.sandbox.tool_registry.ToolRegistry`, validated against `src.sandbox.adapter_base.ToolDescriptor`, and Ed25519-verified against `backend/config/tools/SIGNATURES` before the application starts serving traffic. ARG-040 (Cycle 4 capstone) added the mandatory `version: <semver>` field on every descriptor. Cycle 6 T05 (2026-04-21) ratcheted first-class parser coverage to **mapped: 118 (75.2%) / heartbeat: 39 (24.9%)** — see *Parser coverage* below. ARG-049 (Cycle 5 capstone) ratcheted the per-tool contract matrix from 14 to **16** parametrised gates by adding C15 (`tool-yaml-version-monotonic`, frozen baseline at `backend/tests/snapshots/tool_versions_baseline.json`) and C16 (`image-coverage-completeness`, every tool_id installed by ≥1 of the 6 sandbox image profiles in `infra/sandbox/images/tool_to_package.json`).

Build & run pipeline at a glance:

1. **YAML descriptor** under `backend/config/tools/*.yaml` (matching record in `SIGNATURES`).
2. **`ToolRegistry.load()`** parses through `ToolDescriptor`, verifies the Ed25519 signature, and validates `command_template` against the placeholder allow-list (`src.sandbox.templating.ALLOWED_PLACEHOLDERS`).
3. **`KubernetesSandboxAdapter`** materialises a hardened `Job` manifest (non-root, read-only rootfs, dropped caps, NetworkPolicy from `src.sandbox.network_policies`) and dispatches it.
4. **Findings + evidence** flow into the standard pipeline (`FindingDTO`, `EvidenceDTO`) and are persisted with redaction + chain-of-custody hashing (`src.evidence.pipeline`, `src.evidence.redaction`).

See `Backlog/dev1_md` §4 for the long-term tool roadmap and the intent behind each category, plus §3 / §5 / §18 for the adapter contract, sandbox guardrails, and signing model that this catalog instantiates.

## Tool reference

### Recon (`recon`)

_56 tool(s) in this phase._

| tool_id | category | risk_level | network_policy | requires_approval | parse_strategy | parser_status | command_template | description |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `amass_passive` | recon | passive | recon-passive | no | json_lines | mapped | <code>amass enum -passive -d {domain} -json {out_dir}/amass.jsonl -timeout 20</code> | OWASP Amass passive subdomain enumeration via OSINT only; never queries the t... |
| `aquatone` | recon | passive | recon-passive | no | json_object | heartbeat | <code>aquatone -list {in_dir}/urls.txt -out {out_dir}/aquatone</code> | Aquatone HTTP visual recon and screenshots (image wrapper supplies '-list' sh... |
| `assetfinder` | recon | passive | recon-passive | no | text_lines | mapped | <code>sh -c assetfinder --subs-only {domain} &gt; {out_dir}/assetfinder.txt</code> | tomnomnom assetfinder passive subdomain discovery; one subdomain per line. Ba... |
| `censys` | recon | passive | recon-passive | no | json_object | mapped | <code>sh -c censys search &#x27;services.tls.certificates.leaf_data.names:{domain}&#x27; -o {out_dir}/censys.json</code> | Censys CLI passive search for hosts with TLS certs covering the target domain... |
| `chaos` | recon | passive | recon-passive | no | text_lines | mapped | <code>chaos -d {domain} -o {out_dir}/chaos.txt -silent</code> | ProjectDiscovery chaos passive subdomain dataset; requires CHAOS_API_KEY env.... |
| `crt_sh` | recon | passive | recon-passive | no | custom | heartbeat | <code>sh -c curl -sS --fail-with-body --max-time 60 &#x27;https://crt.sh/?q=%25.{domain}&amp;output=json&#x27; -o {out_dir}/crtsh.json</code> | Certificate Transparency lookup via crt.sh; passive subdomain enumeration wit... |
| `dnsrecon` | recon | passive | recon-passive | no | json_object | mapped | <code>dnsrecon -d {domain} -t std,brt,srv,axfr -j {out_dir}/dnsrecon.json</code> | dnsrecon multi-mode lookup (std/brt/srv/axfr) emitting one JSON object with d... |
| `dnsx` | recon | passive | recon-passive | no | json_lines | mapped | <code>dnsx -l {in_dir}/subs.txt -a -aaaa -cname -mx -ns -txt -resp -json -o {out_dir}/dnsx.json</code> | ProjectDiscovery dnsx fast DNS resolver for A/AAAA/CNAME/MX/NS/TXT records ov... |
| `enum4linux_ng` | network | low | recon-smb | no | json_object | mapped | <code>enum4linux-ng -A -oJ {out_dir}/enum4linux.json {ip}</code> | enum4linux-ng full SMB/NetBIOS enumeration including users, groups, shares, a... |
| `eyewitness` | recon | passive | recon-passive | no | json_object | heartbeat | <code>eyewitness -f {in_dir}/urls.txt -d {out_dir}/eyewitness --no-prompt --web</code> | EyeWitness web screenshots and HTTP report (--no-prompt --web). Backlog/dev1_... |
| `favfreak` | recon | passive | recon-passive | no | json_object | heartbeat | <code>favfreak -l {in_dir}/urls.txt -o {out_dir}/favfreak.json</code> | FavFreak favicon-hash fingerprinting (Shodan/Censys lookups). Backlog/dev1_md... |
| `ffuf_vhost` | recon | low | recon-active-tcp | no | json_object | mapped | <code>ffuf -u https://{ip} -H Host: FUZZ.{domain} -w {wordlist} -mc all -fs {size} -o {out_dir}/ffuf_vhost.json -of json</code> | ffuf vhost discovery via Host header fuzzing. Caller MUST pass {size} as the... |
| `fierce` | recon | passive | recon-passive | no | json_object | mapped | <code>fierce --domain {domain} --json {out_dir}/fierce.json</code> | fierce DNS recon walking adjacent IPs and brute-forcing hostnames within the... |
| `findomain` | recon | passive | recon-passive | no | text_lines | mapped | <code>findomain -t {domain} -u {out_dir}/findomain.txt</code> | Findomain passive subdomain discovery via Certificate Transparency and OSINT.... |
| `gau` | recon | passive | recon-passive | no | json_lines | mapped | <code>gau --threads 5 --json --o {out_dir}/gau.json {domain}</code> | GetAllUrls (gau) fetches archived URLs for {domain} from the Wayback Machine,... |
| `github_search` | recon | passive | recon-passive | no | custom | heartbeat | <code>sh -c gh api -X GET search/code -f q=org:{org} &gt; {out_dir}/github_search.json</code> | GitHub code search for accidental secret/key leaks within the target organisa... |
| `gospider` | recon | low | recon-active-tcp | no | json_lines | mapped | <code>gospider -s {url} -c 10 -d 3 --js --sitemap --robots --json -o {out_dir}/gospider</code> | Gospider fast Go crawler — concurrency 10, depth 3, follows JS / sitemap.xml... |
| `gowitness` | recon | passive | recon-passive | no | json_object | mapped | <code>gowitness file -f {in_dir}/urls.txt --destination {out_dir}/screens</code> | gowitness headless-Chrome screenshots from a URL list. Backlog/dev1_md §4.4. |
| `graphw00f` | web_va | low | recon-active-tcp | no | json_object | heartbeat | <code>graphw00f -d -t {url} -o {out_dir}/graphw00f.json</code> | graphw00f GraphQL fingerprinter — sends 12 small introspection-friendly queri... |
| `grpcurl_probe` | network | low | recon-active-tcp | no | text_lines | heartbeat | <code>sh -c grpcurl -plaintext {host}:{port} list &gt; {out_dir}/grpc.txt</code> | grpcurl reflection-based service enumerator — issues one `list` reflection ca... |
| `hakrawler` | recon | low | recon-active-tcp | no | text_lines | mapped | <code>hakrawler-wrapper -url {url} -depth 3 -subs -out {out_dir}/hakrawler.txt</code> | hakrawler subdomain + endpoint crawler; depth 3, follows subdomains. Image-si... |
| `httpx` | recon | passive | recon-passive | no | json_lines | mapped | <code>httpx -l {in_dir}/urls.txt -json -title -tech-detect -status-code -tls-probe -favicon -jarm -o {out_dir}/httpx.json</code> | ProjectDiscovery httpx HTTP fingerprinting (status, title, tech, TLS, favicon... |
| `ike_scan` | network | low | auth-bruteforce | no | text_lines | heartbeat | <code>sh -c ike-scan -M -A {host} &gt; {out_dir}/ike_scan.txt</code> | ike-scan — IKEv1/v2 IPsec endpoint discovery against {host} (UDP/500). -A ena... |
| `jarm` | recon | passive | recon-passive | no | json_object | mapped | <code>jarm -i {in_dir}/urls.txt -j {out_dir}/jarm.json</code> | JARM TLS server fingerprinting from a URL list. Backlog/dev1_md §4.4. |
| `katana` | recon | low | recon-active-tcp | no | json_lines | mapped | <code>katana -u {url} -jc -d 3 -rl 50 -jsonl -output {out_dir}/katana.jsonl</code> | ProjectDiscovery katana headless crawler — depth 3, rate-limited 50 req/s, JS... |
| `ldapsearch` | network | low | auth-bruteforce | no | text_lines | mapped | <code>sh -c ldapsearch -x -H ldap://{host} -b {basedn} -s sub &gt; {out_dir}/ldapsearch.txt</code> | OpenLDAP ldapsearch — anonymous LDAP enumeration (-x simple bind) against {ho... |
| `linkfinder` | recon | passive | recon-passive | no | text_lines | mapped | <code>linkfinder-wrapper -i {url} -o cli -out {out_dir}/linkfinder.txt</code> | LinkFinder extracts endpoint references from JavaScript bundles served at {ur... |
| `masscan` | network | low | recon-active-tcp | no | json_object | mapped | <code>masscan -p1-65535 --rate 10000 -oJ {out_dir}/masscan.json {cidr}</code> | masscan high-speed TCP port discovery across the in-scope CIDR; rate-limited... |
| `mongodb_probe` | network | low | auth-bruteforce | no | text_lines | mapped | <code>sh -c mongosh --host {host} --port {port} --quiet --eval &#x27;JSON.stringify({serverStatus: db.serverStatus({asserts:0,connections:1,host:1,version:1}), dbs: db.adminCommand({listDatabases:1, nameOnly:true})})&#x27; &gt; {out_dir}/mongo_info.json</code> | mongosh probe — anonymous MongoDB enumeration against {host}:{port}. Read-onl... |
| `naabu` | network | low | recon-active-tcp | no | json_lines | mapped | <code>naabu -host {ip} -p - -json -o {out_dir}/naabu.json</code> | ProjectDiscovery naabu full-range TCP SYN port scan emitting JSONL one entry... |
| `nmap_tcp_full` | network | low | recon-active-tcp | no | xml_nmap | mapped | <code>nmap -sS -Pn -p- -T4 --min-rate 2000 -oX {out_dir}/nmap_full.xml {ip}</code> | Nmap TCP SYN scan across all 65535 ports with rate-limited probes; full port-... |
| `nmap_tcp_top` | network | low | recon-active-tcp | no | xml_nmap | mapped | <code>nmap -sS -Pn -T4 --top-ports 1000 -oX {out_dir}/nmap_tcp.xml {ip}</code> | Nmap TCP SYN scan against the 1000 most common ports; baseline service exposu... |
| `nmap_udp` | network | low | recon-active-udp | no | xml_nmap | mapped | <code>nmap -sU --top-ports 100 -oX {out_dir}/nmap_udp.xml {ip}</code> | Nmap UDP scan across the 100 most common UDP ports; complements TCP discovery... |
| `nmap_version` | network | low | recon-active-tcp | no | xml_nmap | mapped | <code>nmap -sV -sC -p{ports} --script=default,safe -oX {out_dir}/nmap_v.xml {ip}</code> | Nmap service-version detection plus default+safe NSE scripts on previously-di... |
| `onesixtyone` | network | low | auth-bruteforce | no | text_lines | heartbeat | <code>sh -c onesixtyone -c {wordlist} -i {in_dir}/hosts.txt -o {out_dir}/onesixtyone.txt</code> | onesixtyone — fast SNMP community string scanner; UDP/161 sweep against the h... |
| `otx_alienvault` | recon | passive | recon-passive | no | custom | heartbeat | <code>sh -c curl -sS --fail-with-body --max-time 60 &#x27;https://otx.alienvault.com/api/v1/indicators/domain/{domain}/passive_dns&#x27; -o {out_dir}/otx.json</code> | AlienVault OTX passive DNS lookup for historical resolutions of the target do... |
| `paramspider` | recon | passive | recon-passive | no | text_lines | mapped | <code>paramspider -d {domain} -o {out_dir}/paramspider.txt</code> | paramspider mines URL parameters from the Wayback Machine archive (passive, n... |
| `puppeteer_screens` | browser | passive | recon-passive | no | json_object | mapped | <code>sh -c puppeteer-screens --url {url} --output {out_dir}/puppeteer</code> | Puppeteer-driven full-page screenshot + DOM snapshot of {url}. Headless Chrom... |
| `redis_cli_probe` | network | low | auth-bruteforce | no | text_lines | mapped | <code>sh -c redis-cli -h {host} -p {port} INFO &gt; {out_dir}/redis_info.txt &amp;&amp; redis-cli -h {host} -p {port} CONFIG GET &#x27;*&#x27; &gt;&gt; {out_dir}/redis_info.txt</code> | redis-cli probe — INFO + CONFIG GET against an unauthenticated Redis instance... |
| `rpcclient_enum` | network | low | recon-smb | no | text_lines | mapped | <code>sh -c rpcclient -U &#x27;&#x27; -N {ip} -c &#x27;enumdomusers; enumdomgroups; querydominfo&#x27; &gt; {out_dir}/rpc.txt</code> | rpcclient null-session enumeration of domain users, groups, and basic domain... |
| `rustscan` | network | low | recon-active-tcp | no | xml_nmap | heartbeat | <code>sh -c rustscan -a {ip} --ulimit 5000 -- -sC -sV -oX {out_dir}/rustscan.xml</code> | RustScan fast port-discovery handing results to nmap -sC -sV for service fing... |
| `securitytrails` | recon | passive | recon-passive | no | custom | heartbeat | <code>sh -c curl -sS --fail-with-body --max-time 60 -H &quot;APIKEY: ${SECURITYTRAILS_API_KEY:?missing}&quot; &#x27;https://api.securitytrails.com/v1/domain/{domain}/subdomains&#x27; -o {out_dir}/securitytrails.json</code> | SecurityTrails passive subdomain lookup via the v1 REST API. Requires SECURIT... |
| `shodan_cli` | recon | passive | recon-passive | no | csv | heartbeat | <code>sh -c shodan search --fields ip_str,port,org,hostnames hostname:{domain} &gt; {out_dir}/shodan.csv</code> | Shodan CLI passive lookup of exposed services for hostnames matching the targ... |
| `smbmap` | network | low | recon-smb | no | text_lines | mapped | <code>sh -c smbmap -H {ip} -R -q &gt; {out_dir}/smbmap.txt</code> | smbmap recursively enumerates SMB shares and access rights using anonymous cr... |
| `snmp_check` | auth | low | auth-bruteforce | no | text_lines | heartbeat | <code>sh -c snmp-check -c {community} {host} &gt; {out_dir}/snmp.txt</code> | snmp-check — SNMPv1/v2c walk against {host} using {community} string (default... |
| `snmpwalk` | network | low | auth-bruteforce | no | text_lines | mapped | <code>sh -c snmpwalk -v 2c -c {community} -t 5 -r 2 {host} &gt; {out_dir}/snmpwalk.txt</code> | Net-SNMP snmpwalk — full SNMPv2c MIB walk against {host} using {community}. R... |
| `subfinder` | recon | passive | recon-passive | no | json_object | mapped | <code>subfinder -d {domain} -all -silent -oJ -o {out_dir}/subfinder.json</code> | ProjectDiscovery subfinder passive subdomain enumeration via OSINT sources. B... |
| `subjs` | recon | passive | recon-passive | no | text_lines | mapped | <code>subjs-wrapper -i {in_dir}/urls.txt -out {out_dir}/subjs.txt</code> | subjs extracts JavaScript file URLs referenced by the URLs in `{in_dir}/urls.... |
| `theharvester` | recon | passive | recon-passive | no | custom | heartbeat | <code>theHarvester -d {domain} -b all -f {out_dir}/theharvester.json</code> | theHarvester OSINT collection (emails, hosts, IPs) across all configured pass... |
| `unicornscan` | network | low | recon-active-udp | no | text_lines | mapped | <code>unicornscan -mT {ip}:a -I -l {out_dir}/unicornscan.txt</code> | unicornscan asynchronous TCP/UDP port discovery with PCAP-style logging. Back... |
| `urlscan` | recon | passive | recon-passive | no | custom | heartbeat | <code>sh -c curl -sS --fail-with-body --max-time 60 -H &quot;API-Key: ${URLSCAN_API_KEY:?missing}&quot; -H &#x27;Content-Type: application/json&#x27; -X POST &#x27;https://urlscan.io/api/v1/scan/&#x27; -d &#x27;{&quot;url&quot;:&quot;{url}&quot;,&quot;visibility&quot;:&quot;unlisted&quot;}&#x27; -o {out_dir}/urlscan.json</code> | urlscan.io passive submission of the target URL; rendered DOM/screens fetched... |
| `wappalyzer_cli` | recon | passive | recon-passive | no | json_object | mapped | <code>wappalyzer {url} --pretty</code> | Wappalyzer CLI tech-stack detection (stdout JSON; image wrapper persists to /... |
| `waybackurls` | recon | passive | recon-passive | no | text_lines | mapped | <code>waybackurls-wrapper -domain {domain} -out {out_dir}/wayback.txt</code> | waybackurls fetches every historical URL ever recorded by the Internet Archiv... |
| `webanalyze` | recon | passive | recon-passive | no | json_object | mapped | <code>webanalyze -host {host} -output json</code> | webanalyze (Wappalyzer-port) tech-stack scan (stdout JSON; image wrapper pers... |
| `whatweb` | recon | passive | recon-passive | no | json_object | mapped | <code>whatweb -a 3 --log-json {out_dir}/whatweb.json {url}</code> | WhatWeb aggressive (level 3) tech fingerprinting. Backlog/dev1_md §4.4. |
| `whois_rdap` | recon | passive | recon-passive | no | custom | heartbeat | <code>sh -c whois {domain} &gt; {out_dir}/whois.txt ; curl -sS --fail-with-body --max-time 30 https://rdap.org/domain/{domain} -o {out_dir}/rdap.json</code> | Combined classic WHOIS + RFC 9082 RDAP lookup for registrant, NS, and contact... |

### Vulnerability analysis (`vuln_analysis`)

_79 tool(s) in this phase._

| tool_id | category | risk_level | network_policy | requires_approval | parse_strategy | parser_status | command_template | description |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `apktool` | binary | low | offline-no-egress | no | text_lines | mapped | <code>sh -c apktool d -f -o {out_dir}/apktool {binary} 2&gt;&amp;1 &#124; tee {out_dir}/apktool.log</code> | Apktool — Android APK reverse-engineering. Decompiles {binary} into smali + A... |
| `arachni` | web_va | medium | recon-active-tcp | yes | text_lines | mapped | <code>arachni {url} --scope-include-subdomains --scope-page-limit 500 --http-request-concurrency 10 --report-save-path {out_dir}/arachni.afr --checks=* --timeout 00:25:00</code> | Arachni full-featured web application security scanner — crawls {url} (subdom... |
| `arjun` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>arjun -u {url} -oJ {out_dir}/arjun.json -m GET,POST</code> | arjun HTTP parameter discovery via reflection / behaviour analysis. Author: s... |
| `bandit` | misc | passive | offline-no-egress | no | json_object | mapped | <code>bandit -r {path} -f json -o {out_dir}/bandit.json -q</code> | PyCQA Bandit Python SAST — recursively analyses the Python source tree at {pa... |
| `binwalk` | binary | low | offline-no-egress | no | text_lines | mapped | <code>sh -c binwalk -e --run-as=root --directory={out_dir}/binwalk {file} 2&gt;&amp;1 &#124; tee {out_dir}/binwalk.log</code> | Binwalk — firmware / ELF / PE blob signature scanner. -e auto-extracts every... |
| `checkov` | iac | passive | offline-no-egress | no | json_object | mapped | <code>sh -c checkov -d {path} -o json --quiet &gt; {out_dir}/checkov.json</code> | Bridgecrew Checkov IaC static analyser — scans the IaC bundle at {path} (Terr... |
| `chrome_csp_probe` | browser | low | recon-active-tcp | no | json_object | mapped | <code>sh -c chrome-csp-probe --url {url} --output {out_dir}/csp.json</code> | Headless Chromium Content-Security-Policy probe — loads {url} and emits the m... |
| `clairvoyance` | web_va | low | recon-active-tcp | no | json_object | heartbeat | <code>clairvoyance {url}/graphql -o {out_dir}/clairvoyance.json</code> | clairvoyance GraphQL field-suggestion oracle — reconstructs the schema (field... |
| `cloudsploit` | cloud | medium | recon-passive | yes | json_object | mapped | <code>cloudsploit scan --config {path} --json {out_dir}/cloudsploit.json</code> | Aqua CloudSploit multi-cloud posture scan — operator config bundle at {path}... |
| `cmsmap` | web_va | low | recon-active-tcp | no | text_lines | mapped | <code>cmsmap {url} -F -o {out_dir}/cmsmap.txt</code> | CMSmap multi-CMS scanner (WordPress/Joomla/Drupal/Moodle) — force-detect mode... |
| `cookie_probe` | browser | low | recon-active-tcp | no | json_object | heartbeat | <code>sh -c cookie-probe --url {url} --output {out_dir}/cookies.json</code> | Headless cookie attribute auditor — drives the {url} login / landing flow wit... |
| `cors_probe` | browser | low | recon-active-tcp | no | json_object | heartbeat | <code>sh -c cors-probe --url {url} --output {out_dir}/cors.json</code> | CORS misconfiguration probe — fires a curated Origin-header matrix at {url} (... |
| `dalfox` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>dalfox url {url} --mining-dom --mining-dict --deep-domxss --skip-bav --no-color --format json --output {out_dir}/dalfox.json --worker 10 --timeout 10</code> | dalfox parameter-aware XSS scanner — DOM mining (--mining-dom), payload minin... |
| `detect_secrets` | misc | passive | offline-no-egress | no | json_object | mapped | <code>sh -c detect-secrets scan {path} &gt; {out_dir}/detect-secrets.json</code> | Yelp detect-secrets entropy + plugin-based secret scanner — analyses the sour... |
| `dirsearch` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>dirsearch -u {url} -e php,aspx,js,html -o {out_dir}/dirsearch.json --format json</code> | dirsearch directory / file discovery with extension fuzzing, JSON output. Aut... |
| `dockle` | cloud | passive | recon-passive | no | json_object | mapped | <code>dockle -f json -o {out_dir}/dockle.json {image}</code> | Goodwith Dockle container best-practice checker — runs the CIS Docker Benchma... |
| `droopescan` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>droopescan scan --url {url} --enumerate a --output json --threads 8 --output-file {out_dir}/droopescan.json</code> | Droopescan multi-CMS scanner (Drupal/Joomla/SilverStripe/WordPress) — full en... |
| `feroxbuster` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>feroxbuster -u {url} -w {wordlist} -d 3 -x php,aspx,js,txt --json -o {out_dir}/ferox.json</code> | feroxbuster recursive directory / file content discovery, JSON output. Author... |
| `ffuf_dir` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>ffuf -u {url}/FUZZ -w {wordlist} -mc 200,204,301,302,307,401,403 -recursion -recursion-depth 2 -o {out_dir}/ffuf_dir.json -of json</code> | ffuf directory / file content discovery with recursion depth 2. Author: ffuf... |
| `ffuf_param` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>ffuf -u {url}?FUZZ=test -w {wordlist} -mc all -fs {size} -o {out_dir}/ffuf_param.json -of json</code> | ffuf GET parameter discovery via FUZZ=test injection. Caller MUST pass {size}... |
| `ghauri` | web_va | medium | recon-active-tcp | yes | text_lines | mapped | <code>ghauri -u {url} --batch --level 2 --dbs --timeout 30 --retries 2 --threads 2 --no-logging</code> | ghauri lightweight SQLi detector — Python rewrite of sqlmap's blind/error/UNI... |
| `gitleaks` | misc | passive | offline-no-egress | no | json_object | mapped | <code>gitleaks detect -s {path} -r {out_dir}/gitleaks.json -f json --no-banner --exit-code 0</code> | Gitleaks git-history secret scanner — sweeps the repo at {path} (/in) using 1... |
| `gobuster_auth` | auth | low | recon-active-tcp | no | text_lines | mapped | <code>gobuster fuzz -u {url} -w {wordlist} -t 10 --timeout 10s -o {out_dir}/gobuster_auth.txt</code> | gobuster fuzz mode — HTTP form/cookie/header brute-force using wordlist. Suit... |
| `gobuster_dir` | web_va | low | recon-active-tcp | no | text_lines | mapped | <code>gobuster dir -u {url} -w {wordlist} -x php,txt,html -o {out_dir}/gobuster.txt</code> | gobuster directory / file discovery, plain text output. Author: OJ Reeves (ht... |
| `gopherus` | web_va | low | offline-no-egress | no | text_lines | heartbeat | <code>sh -c gopherus --exploit mysql &gt; {out_dir}/gopher.txt</code> | Gopherus — SSRF payload-template generator (gopher://, ftp://, smtp:// chains... |
| `graphql_cop` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>sh -c graphql-cop -t {url}/graphql -o json &gt; {out_dir}/graphqlcop.json</code> | graphql-cop GraphQL security audit — runs ~15 safety checks against {url}/gra... |
| `grype` | cloud | passive | recon-passive | no | json_object | mapped | <code>sh -c grype {image} -o json --quiet &gt; {out_dir}/grype.json</code> | Anchore Grype vulnerability scanner — pulls {image}, derives the SBOM via bun... |
| `inql` | web_va | low | recon-active-tcp | no | json_object | heartbeat | <code>inql -t {url}/graphql -o {out_dir}/inql.json</code> | inql GraphQL introspection + query-template generator — pulls the schema of {... |
| `interactsh_client` | oast | low | oast-egress | no | json_lines | mapped | <code>interactsh-client -server https://oast.argus.local -json -o {out_dir}/interactsh.jsonl -v</code> | interactsh-client polls the ARGUS OAST plane for OOB callbacks (DNS / HTTP /... |
| `jadx` | binary | low | offline-no-egress | no | text_lines | mapped | <code>sh -c jadx --no-imports --show-bad-code -d {out_dir}/jadx {binary} 2&gt;&amp;1 &#124; tee {out_dir}/jadx.log</code> | JADX — DEX-to-Java decompiler. Translates the DEX bytecode inside {binary} (A... |
| `jenkins_enum` | web_va | low | recon-active-tcp | no | nuclei_jsonl | mapped | <code>nuclei -target {url} -tags jenkins -severity info,low,medium,high,critical -jsonl -output {out_dir}/jenkins_enum.jsonl -disable-update-check -no-color -rate-limit 50 -c 10 -stats</code> | Jenkins enumeration & exposure check — runs nuclei with the `jenkins` tag aga... |
| `joomscan` | web_va | low | recon-active-tcp | no | text_lines | mapped | <code>joomscan --url {url} --ec --report {out_dir}/joomscan.txt</code> | OWASP JoomScan Joomla security scanner — enumerates components (--ec), vulner... |
| `jsql` | web_va | medium | recon-active-tcp | yes | json_object | mapped | <code>jsql-cli --url {url} --get-data --output {out_dir}/jsql.json</code> | jsql-injection (Java) headless wrapper — /usr/local/bin/jsql-cli drives 'java... |
| `kics` | iac | passive | offline-no-egress | no | json_object | mapped | <code>kics scan -p {path} -o {out_dir} --report-formats json --no-progress</code> | Checkmarx KICS (Keeping Infrastructure as Code Secure) — multi-IaC scanner co... |
| `kiterunner` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>kr scan {url} -w {wordlist} -o {out_dir}/kr.json</code> | kiterunner (kr) API route discovery using Swagger / OpenAPI wordlists. Author... |
| `kube_bench` | cloud | passive | offline-no-egress | no | json_object | mapped | <code>sh -c kube-bench --json &gt; {out_dir}/kubebench.json</code> | Aqua kube-bench CIS Kubernetes Benchmark scanner — runs the local-node assess... |
| `kube_hunter` | cloud | medium | recon-active-tcp | yes | json_object | heartbeat | <code>sh -c kube-hunter --remote {host} --report json --log none &gt; {out_dir}/kubehunter.json</code> | Aqua kube-hunter remote Kubernetes attack-surface scanner — sends a fixed cat... |
| `kxss` | web_va | passive | recon-active-tcp | no | text_lines | mapped | <code>kxss-runner --url {url} --output {out_dir}/kxss.txt</code> | kxss reflected-XSS surface enumerator — image ships /usr/local/bin/kxss-runne... |
| `magescan` | web_va | low | recon-active-tcp | no | text_lines | mapped | <code>magescan scan:all {url} --format=json --no-ansi --quiet</code> | MageScan Magento e-commerce security scanner — scan:all surface (version, mod... |
| `mkcert_verify` | web_va | low | tls-handshake | no | custom | heartbeat | <code>sh -c openssl s_client -connect {host}:{port} -servername {host} -showcerts &lt;/dev/null 2&gt;/dev/null &gt; {out_dir}/chain.pem &amp;&amp; openssl verify -CApath /etc/ssl/certs {out_dir}/chain.pem &gt; {out_dir}/mkcert_verify.txt 2&gt;&amp;1</code> | Local TLS chain validation: pulls the live certificate chain via openssl s_cl... |
| `mobsf_api` | binary | low | offline-no-egress | no | json_object | mapped | <code>sh -c mobsf-cli --apk {binary} --output {out_dir}/mobsf.json</code> | Mobile Security Framework (MoBSF) CLI — static analysis of an Android APK / i... |
| `nextjs_check` | web_va | low | recon-active-tcp | no | nuclei_jsonl | mapped | <code>nuclei -target {url} -tags nextjs -severity info,low,medium,high,critical -jsonl -output {out_dir}/nextjs_check.jsonl -disable-update-check -no-color -rate-limit 50 -c 10 -stats</code> | Next.js exposure check — runs nuclei with the `nextjs` tag set against {url}.... |
| `nikto` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>nikto -host {url} -Format json -output {out_dir}/nikto.json -ask no -nointeractive -Tuning 1234567890abcde -maxtime 1500s</code> | Nikto2 web server scanner — broad checks for ~7000 dangerous files, outdated... |
| `nmap_vuln` | network | medium | recon-active-tcp | no | xml_nmap | mapped | <code>nmap -sV -T2 --script vuln,vulners,vulscan --script-args=mincvss=4.0 -p{ports} -oX {out_dir}/nmap_vuln.xml {ip}</code> | Nmap NSE vuln-category scripts on confirmed ports with -sV service fingerprin... |
| `nosqlmap` | web_va | medium | recon-active-tcp | yes | text_lines | mapped | <code>nosqlmap-runner --target {url} --attack 2 --output {out_dir}/nosqlmap.txt</code> | NoSQLMap Mongo/CouchDB injection probe — /usr/local/bin/nosqlmap-runner wraps... |
| `nuclei` | web_va | low | recon-active-tcp | no | nuclei_jsonl | mapped | <code>nuclei -target {url} -severity info,low,medium,high,critical -jsonl -output {out_dir}/nuclei.jsonl -disable-update-check -no-color -rate-limit 50 -c 10 -stats</code> | ProjectDiscovery nuclei flagship template-driven vulnerability scanner — runs... |
| `oast_dns_probe` | oast | low | oast-egress | no | text_lines | heartbeat | <code>sh -c dig +short +time=5 {rand}.oast.argus.local &gt; {out_dir}/oast_dns.txt</code> | Out-of-band DNS probe — fires a unique {rand}.oast.argus.local query so the O... |
| `oastify_client` | oast | low | oast-egress | no | json_lines | mapped | <code>oastify-client -server https://oast.argus.local -json -o {out_dir}/interactsh.jsonl -v</code> | oastify-client — wire-compatible mirror of interactsh that polls the ARGUS OA... |
| `openapi_scanner` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>openapi-scanner --target {url} --output {out_dir}/openapi.json</code> | openapi_scanner — internal Semgrep-style OpenAPI walker that derives an API s... |
| `playwright_runner` | browser | medium | recon-active-tcp | yes | json_object | mapped | <code>sh -c playwright-runner --script {script} --url {url} --output {out_dir}/playwright</code> | Generic headless Playwright runner — executes the operator-supplied {script}... |
| `postman_newman` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>newman run {path} -r json --reporter-json-export {out_dir}/newman.json --insecure</code> | newman Postman collection runner — executes the operator-curated collection a... |
| `prowler` | cloud | medium | recon-passive | yes | json_object | mapped | <code>prowler aws -M json -o {out_dir}/prowler.json --profile {profile}</code> | Prowler AWS posture scan — runs the full curated AWS check suite (CIS/NIST/PC... |
| `radare2_info` | binary | low | offline-no-egress | no | json_object | mapped | <code>sh -c r2 -2 -q -A -c &#x27;iIj;ihj;izj;iEj;ilj&#x27; {binary} &gt; {out_dir}/r2_info.json</code> | radare2 (r2) — read-only binary triage. -A runs the auto-analysis pass; the -... |
| `scoutsuite` | cloud | medium | recon-passive | yes | json_object | heartbeat | <code>scout aws --report-dir {out_dir}/scoutsuite --no-browser</code> | ScoutSuite multi-cloud posture scan — sweeps AWS services (IAM, S3, EC2, RDS,... |
| `secretfinder` | web_va | passive | recon-passive | no | text_lines | mapped | <code>secretfinder-wrapper -i {in_dir}/js.txt -o cli -out {out_dir}/secretfinder.txt</code> | SecretFinder scans JavaScript bundles listed in `{in_dir}/js.txt` for hard-co... |
| `semgrep` | misc | passive | offline-no-egress | no | json_object | mapped | <code>semgrep scan --config p/owasp-top-ten --config p/ci --json --output {out_dir}/semgrep.json --metrics off --quiet --timeout 300 {path}</code> | Semgrep multi-language SAST — runs `p/owasp-top-ten` + `p/ci` rule packs agai... |
| `skipfish` | web_va | medium | recon-active-tcp | yes | text_lines | heartbeat | <code>skipfish -o {out_dir}/skipfish -S {wordlist} -LV -m 5 -l 10 -t 10 -r 200000 {url}</code> | skipfish active web app scanner — crawls + brute-forces {url} (5 host-conn, 1... |
| `spring_boot_actuator` | web_va | low | recon-active-tcp | no | nuclei_jsonl | mapped | <code>nuclei -target {url} -tags springboot,actuator -severity info,low,medium,high,critical -jsonl -output {out_dir}/spring_boot_actuator.jsonl -disable-update-check -no-color -rate-limit 50 -c 10 -stats</code> | Spring Boot Actuator exposure check — runs nuclei with `springboot,actuator`... |
| `sqlmap_safe` | web_va | medium | recon-active-tcp | yes | text_lines | mapped | <code>sqlmap -u {url} --batch --level 2 --risk 1 --technique=BT --safe-url={safe} --flush-session --random-agent --timeout=30 --retries=2 --threads=2 --disable-coloring --output-dir={out_dir}/sqlmap</code> | sqlmap conservative passive sweep — boolean+time-based blind only (--techniqu... |
| `ssl_enum_ciphers` | web_va | low | tls-handshake | no | xml_nmap | heartbeat | <code>nmap --script ssl-enum-ciphers -p {port} {host} -oX {out_dir}/ssl_ciphers.xml</code> | Nmap NSE ssl-enum-ciphers script: per-protocol cipher table with strength gra... |
| `sslscan` | web_va | low | tls-handshake | no | xml_generic | heartbeat | <code>sslscan --xml={out_dir}/sslscan.xml {host}:{port}</code> | sslscan enumerates supported TLS/SSL ciphers and protocol versions, emitting... |
| `sslyze` | web_va | low | tls-handshake | no | json_object | heartbeat | <code>sslyze --json_out {out_dir}/sslyze.json {host}:{port}</code> | sslyze structured TLS scan covering protocol support, cipher suites, certific... |
| `ssrfmap` | web_va | medium | oast-egress | yes | text_lines | heartbeat | <code>sh -c ssrfmap -r {in_dir}/req.txt -p {params} -m readfiles,portscan -o {out_dir}/ssrfmap.txt</code> | SSRFmap — automated SSRF exploitation pipeline that walks a captured request... |
| `syft` | cloud | passive | recon-passive | no | json_object | mapped | <code>sh -c syft {image} -o cyclonedx-json --quiet &gt; {out_dir}/sbom.json</code> | Anchore Syft SBOM generator — pulls {image} and emits a CycloneDX SBOM (compo... |
| `terrascan` | iac | passive | offline-no-egress | no | json_object | mapped | <code>sh -c terrascan scan -i terraform -d {path} -o json &gt; {out_dir}/terrascan.json</code> | Tenable Terrascan IaC scanner — analyses the Terraform module tree at {path}... |
| `testssl` | web_va | low | tls-handshake | no | json_object | heartbeat | <code>testssl.sh --jsonfile {out_dir}/testssl.json --severity LOW {host}:{port}</code> | testssl.sh comprehensive TLS/SSL configuration audit (ciphers, protocols, vul... |
| `tfsec` | iac | passive | offline-no-egress | no | json_object | mapped | <code>sh -c tfsec {path} --format json --soft-fail &gt; {out_dir}/tfsec.json</code> | Aqua tfsec Terraform static analyser — scans the Terraform module tree at {pa... |
| `tlsx` | web_va | low | tls-handshake | no | json_object | heartbeat | <code>tlsx -host {host} -port {port} -json -o {out_dir}/tlsx.json</code> | ProjectDiscovery tlsx fast TLS handshake summary (cert chain, SAN, protocol,... |
| `tplmap` | web_va | high | recon-active-tcp | yes | text_lines | mapped | <code>tplmap-runner --url {url} --level 3 --os-cmd id --output {out_dir}/tplmap.txt</code> | tplmap SSTI probe — /usr/local/bin/tplmap-runner wraps upstream tplmap.py (in... |
| `trivy_fs` | cloud | passive | recon-passive | no | json_object | mapped | <code>trivy fs --format json -o {out_dir}/trivy_fs.json --scanners vuln,config,secret --severity UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL --no-progress --quiet {path}</code> | Aqua Trivy filesystem scanner — sweeps the source tree mounted at {path} (und... |
| `trivy_image` | cloud | passive | recon-passive | no | json_object | mapped | <code>trivy image --format json -o {out_dir}/trivy.json --scanners vuln,config,secret --severity UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL --no-progress --quiet {image}</code> | Aqua Trivy container image scanner — pulls {image} from the registry and scan... |
| `trufflehog` | misc | passive | offline-no-egress | no | json_lines | mapped | <code>sh -c trufflehog filesystem {path} --json --no-update --no-verification &gt; {out_dir}/trufflehog.json</code> | Truffle Security trufflehog secret scanner — sweeps the source tree at {path}... |
| `w3af_console` | web_va | medium | recon-active-tcp | yes | text_lines | heartbeat | <code>w3af_console -y -n -s {in_dir}/w3af.profile -o {out_dir}/w3af.txt</code> | w3af Web Application Attack & Audit Framework console — runs the user-supplie... |
| `wapiti` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>wapiti -u {url} -f json -o {out_dir}/wapiti.json --flush-session --scope domain --max-scan-time 1500 --verify-ssl 0 -m all</code> | Wapiti3 black-box web vulnerability scanner — crawls and audits {url} for XSS... |
| `wfuzz` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>wfuzz -c -z file,{wordlist} --hc 404 -o json -f {out_dir}/wfuzz.json,json {url}/FUZZ</code> | wfuzz HTTP parameter / path fuzzer with 404 hidden. Stdout printer (-o json)... |
| `wpscan` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>wpscan --url {url} --enumerate vp,vt,u --random-user-agent --detection-mode mixed --no-banner --no-update --disable-tls-checks --format json --output {out_dir}/wpscan.json</code> | WPScan WordPress security scanner — enumerates vulnerable plugins/themes (vp,... |
| `xsser` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>xsser -u {url} --auto --no-color --Json {out_dir}/xsser.json</code> | xsser OWASP Cross-Site Scripter — runs --auto mode (default vector chain) aga... |
| `xsstrike` | web_va | low | recon-active-tcp | no | json_object | mapped | <code>xsstrike-runner --url {url} --crawl --skip-poc --output {out_dir}/xsstrike.json</code> | XSStrike polyglot XSS scanner — /usr/local/bin/xsstrike-runner wraps upstream... |
| `zap_baseline` | web_va | low | recon-active-tcp | no | text_lines | mapped | <code>zap-baseline.py -t {url} -J {out_dir}/zap_baseline.json -r {out_dir}/zap_baseline.html -x {out_dir}/zap_baseline.xml -T 25 -z -config api.disablekey=true -d</code> | OWASP ZAP baseline.py — passive-only scan of {url} (no active payloads), 25 m... |

### Exploitation (`exploitation`)

_14 tool(s) in this phase._

| tool_id | category | risk_level | network_policy | requires_approval | parse_strategy | parser_status | command_template | description |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `cloud_metadata_check` | oast | medium | recon-active-tcp | yes | text_lines | heartbeat | <code>cloud_metadata_check --target {url} --clouds aws,gcp,azure --output {out_dir}/cloud_metadata_check.txt --timeout 5</code> | Cloud metadata SSRF probe — tests AWS IMDSv1/v2 (169.254.169.254), GCE metada... |
| `crackmapexec` | auth | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c crackmapexec smb {host} -u {user} -p {pass} --shares &gt; {out_dir}/cme.txt</code> | CrackMapExec (CME) — Active Directory / SMB credential validator + share enum... |
| `hydra` | auth | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c hydra -L {in_dir}/users.txt -P {in_dir}/pass.txt -t 4 -I -f -o {out_dir}/hydra.txt {target_proto}://{host}:{port}</code> | THC-Hydra credential bruteforce against {target_proto}://{host}:{port}. APPRO... |
| `impacket_examples` | auth | high | auth-bruteforce | yes | text_lines | heartbeat | <code>sh -c GetUserSPNs.py {domain}/{user}:{pass} -dc-ip {dc} -request -outputfile {out_dir}/spns.txt</code> | Impacket GetUserSPNs.py — Kerberoasting service-principal enumeration that re... |
| `kerbrute` | auth | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c kerbrute userenum --dc {dc} -d {domain} {in_dir}/users.txt -o {out_dir}/kerbrute.txt</code> | Kerbrute — Kerberos username enumeration via AS-REQ pre-auth probe (no lockou... |
| `medusa` | auth | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c medusa -h {host} -U {in_dir}/u.txt -P {in_dir}/p.txt -M {mod} -O {out_dir}/medusa.txt</code> | Medusa parallel credential bruteforcer driven by the per-protocol module {mod... |
| `ncrack` | auth | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c ncrack -U {in_dir}/u.txt -P {in_dir}/p.txt -oN {out_dir}/ncrack.txt {host}:{port}</code> | Ncrack high-speed network auth cracker (Nmap-family, supports SSH/RDP/FTP/Tel... |
| `ntlmrelayx` | network | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c ntlmrelayx.py -t smb://{host} -smb2support -of {out_dir}/relay 2&gt;&amp;1 &#124; tee {out_dir}/relay.log</code> | Impacket ntlmrelayx.py — NTLM authentication relay against {host} (SMBv2). AP... |
| `pacu` | cloud | high | recon-passive | yes | text_lines | heartbeat | <code>pacu --session {s} --module {module}</code> | Pacu AWS exploitation framework — runs {module} (e.g. iam__enum_permissions)... |
| `patator` | auth | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c patator {module} host={host} user=FILE0 password=FILE1 0={in_dir}/u.txt 1={in_dir}/p.txt -x ignore:code=401 &gt; {out_dir}/patator.log</code> | Patator multi-module credential bruteforcer (ssh_login, ftp_login, http_fuzz,... |
| `playwright_xss_verify` | browser | low | recon-active-tcp | no | json_object | mapped | <code>playwright-verify-xss --url {url} --canary {canary} --output {out_dir}/playwright.json --screenshot {out_dir}/playwright.png</code> | Headless Playwright XSS verifier — /usr/local/bin/playwright-verify-xss (Node... |
| `responder` | network | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c responder -I {interface} -A -v 2&gt;&amp;1 &#124; tee {out_dir}/responder.log</code> | Responder — LLMNR/NBT-NS/MDNS poisoner that captures NTLMv1/NTLMv2 challenge-... |
| `smbclient` | auth | medium | auth-bruteforce | yes | text_lines | mapped | <code>sh -c smbclient -L //{host} -U {user}%{pass} &gt; {out_dir}/smb_list.txt</code> | smbclient -L — authenticated SMB share listing using the supplied (user, pass... |
| `sqlmap_confirm` | web_va | high | recon-active-tcp | yes | text_lines | mapped | <code>sqlmap -u {url} --batch --technique=E --dbs --count --random-agent --timeout=30 --retries=2 --threads=4 --disable-coloring --output-dir={out_dir}/sqlmap_confirm</code> | sqlmap confirmation pass — error-based exploitation (--technique=E) + DBMS sc... |

### Post-exploitation (`post_exploitation`)

_8 tool(s) in this phase._

| tool_id | category | risk_level | network_policy | requires_approval | parse_strategy | parser_status | command_template | description |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `bloodhound_python` | network | medium | auth-bruteforce | yes | text_lines | mapped | <code>sh -c bloodhound-python -d {domain} -u {user} -p {pass} -dc {dc} -c All -ns {dc} --zip -o {out_dir}/bloodhound 2&gt;&amp;1 &#124; tee {out_dir}/bloodhound.log</code> | BloodHound.py — Python-based Active Directory data collector (-c All gathers... |
| `evil_winrm` | auth | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c evil-winrm -i {host} -u {user} -p {pass} -s {in_dir}/ &gt; {out_dir}/winrm.log</code> | evil-winrm — authenticated WinRM remote-shell client used for post-exploitati... |
| `hash_analyzer` | misc | low | offline-no-egress | no | json_object | mapped | <code>sh -c hash-analyzer --in {hashes_file} --json &gt; {out_dir}/hash_analyzer.json</code> | hash-analyzer — extended hash classification (HMAC, salts, encoding chains).... |
| `hashcat` | misc | high | offline-no-egress | yes | text_lines | mapped | <code>sh -c hashcat -m {mode} -a 0 -o {out_dir}/cracked.txt {hashes_file} {wordlist}</code> | Hashcat — GPU/CPU offline hash cracker. APPROVAL REQUIRED: long-running and C... |
| `hashid` | misc | low | offline-no-egress | no | json_object | mapped | <code>sh -c hashid -m -j {hashes_file} &gt; {out_dir}/hashid.json</code> | hashid — local hash-format identifier (no egress, fail-safe). Walks {hashes_f... |
| `impacket_secretsdump` | network | high | auth-bruteforce | yes | text_lines | mapped | <code>sh -c secretsdump.py {domain}/{user}:{pass}@{dc} -outputfile {out_dir}/secrets</code> | Impacket secretsdump.py — DRSUAPI / NTDS / SAM / LSA secret extraction agains... |
| `john` | misc | high | offline-no-egress | yes | text_lines | heartbeat | <code>sh -c john --format={fmt} --wordlist={wordlist} {hashes_file} &amp;&amp; john --show {hashes_file} &gt; {out_dir}/john.txt</code> | John the Ripper — offline hash cracker. APPROVAL REQUIRED: long-running and C... |
| `ophcrack` | misc | high | offline-no-egress | yes | text_lines | heartbeat | <code>sh -c ophcrack -d {in_dir}/tables -t xp_free_fast -f {hashes_file} -o {out_dir}/oph.txt</code> | Ophcrack — Windows LM/NTLM rainbow-table cracker. APPROVAL REQUIRED: long-run... |

## Security invariants

Every tool in this catalog is dispatched through the ARGUS sandbox (`backend/src/sandbox/`) which enforces the following invariants — no per-tool override is possible:

- **Non-root pod** — `runAsNonRoot=True`, `runAsUser=runAsGroup=fsGroup=65532` (`src.sandbox.manifest.build_pod_security_context`).
- **Read-only root filesystem** — `readOnlyRootFilesystem=True`; only `/out` and `/tmp` `emptyDir` volumes are writable. No `hostPath`, no PVCs, no docker.sock mounts.
- **All Linux capabilities dropped** — `capabilities.drop=["ALL"]`, `allowPrivilegeEscalation=False`, `privileged=False` (`src.sandbox.manifest.build_container_security_context`).
- **Seccomp `RuntimeDefault`** — every descriptor declares `seccomp_profile=runtime/default`; `ToolRegistry.load()` is fail-closed on any other value.
- **No service-account token** — `automountServiceAccountToken=False`; tool pods cannot reach the Kubernetes API.
- **Argv-only execution** — `subprocess.run(argv, shell=False)`; templates render through `src.sandbox.templating.render_argv` against an allow-listed placeholder set (`ALLOWED_PLACEHOLDERS`).
- **Ed25519-signed YAML descriptors** — every entry is verified against `backend/config/tools/SIGNATURES` (`src.sandbox.signing`); signature mismatch, schema mismatch, duplicate `tool_id`, or a forbidden placeholder all abort startup.
- **Ingress always denied; egress allow-listed per `network_policy.name`** (`src.sandbox.network_policies`). DNS pinned to Cloudflare (`1.1.1.1`) and Quad9 (`9.9.9.9`).
- **Active-recon templates require an explicit `target_cidr`** at render time — wildcard egress to the Internet is impossible from active categories.
- **Job lifecycle** — `restartPolicy=Never`, `backoffLimit=0`, deterministic `default_timeout_s` per descriptor.
- **Guaranteed QoS** — every container declares `requests==limits` for both CPU and memory (defence against `LimitRange` admission and noisy neighbours).

Any descriptor that violates one of these invariants fails `ToolRegistry.load()` and the application refuses to start serving traffic.

## Coverage matrix

Snapshot of the catalog scope shipped through ARG-019 (Backlog/dev1_md §4.1–§4.19) and ratcheted across cycles 2–6 (parser coverage 39 % → 58 % → 62.4 % → **75.2 %** after Cycle 6 T05).  The full Backlog §4 matrix lists **157** tools — ARG-019 closes the long-term catalog at §4.19.  ARG-049 (Cycle 5 capstone) ratchets the per-tool contract matrix to **16** gates (C1–C16); T05 advances the parser ratchet (mapped 118 / heartbeat 39).

| Phase | Shipped | Expected (current scope) | Gap |
| --- | ---: | ---: | ---: |
| `recon` | 56 | 56 | 0 |
| `vuln_analysis` | 79 | 79 | 0 |
| `exploitation` | 14 | 14 | 0 |
| `post_exploitation` | 8 | 8 | 0 |
| **Total** | **157** | **157** | **0** |

## Image coverage

**Built: 152 (96.8%), Pending: 5 (3.2%)** — snapshot at ARG-058 close (cycle 6: dual-listed YAML migration).

Per :attr:`ToolDescriptor.image`, the sandbox materialises one image profile per ``sandbox/images/<name>/Dockerfile``. A descriptor may reference a profile that has not yet been materialised — those rows show ``Dockerfile = no`` and form the operator-visible to-do list for the next image batch. ARG-048 delivered the missing ``argus-kali-recon`` (Backlog §4.1 passive + §4.2 active recon) and ``argus-kali-network`` (Backlog §4.17 protocol exploitation: SNMP/LDAP/SMB/IKE/impacket) profiles, lifting the built-image count from 4 to 6. ARG-058 then moved the 16 dual-listed network-protocol tools (``bloodhound_python``, ``crackmapexec``, ``evil_winrm``, ``ike_scan``, ``impacket_examples``, ``impacket_secretsdump``, ``kerbrute``, ``ldapsearch``, ``mongodb_probe``, ``ntlmrelayx``, ``onesixtyone``, ``redis_cli_probe``, ``responder``, ``smbclient``, ``snmp_check``, ``snmpwalk``) off ``argus-kali-web`` (91 → 75) into ``argus-kali-network`` (0 → 16), eliminating the dual-listing without touching descriptor count.

| image | Dockerfile | tool_count | share |
| --- | :---: | ---: | ---: |
| `argus-kali-web:latest` | yes | 75 | 47.77% |
| `argus-kali-recon:latest` | yes | 29 | 18.47% |
| `argus-kali-cloud:latest` | yes | 26 | 16.56% |
| `argus-kali-network:latest` | yes | 16 | 10.19% |
| `argus-kali-browser:latest` | yes | 6 | 3.82% |
| `argus-kali-binary:latest` | no | 5 | 3.18% |
| **Total** | — | **157** | **100.00%** |

## Parser coverage

**Mapped: 118 (75.2%), Heartbeat: 39 (24.9%)** — snapshot after Cycle 6 T05 (top-20 heartbeat batch).

Per descriptor, ``dispatch_parse`` follows exactly one of three deterministic paths (ARG-020, cycle 2 capstone).  No tool is allowed to silently produce zero findings: an unmapped tool always yields a single ``FindingCategory.INFO`` heartbeat tagged ``ARGUS-HEARTBEAT`` plus a structured ``parsers.dispatch.*`` warning.

- **`mapped`** — `tool_id` is registered in `src.sandbox.parsers._TOOL_TO_PARSER`; the strategy handler routes to a first-class parser that returns real findings.
- **`heartbeat`** — strategy is registered, but no per-tool parser exists yet; the dispatcher emits one heartbeat finding (`FindingCategory.INFO`, CVSS 0.0, `cwe=[1059]`) plus the `parsers.dispatch.unmapped_tool` / `parsers.dispatch.no_handler` WARNING.  Wiring the parser flips the row from `heartbeat` to `mapped` with no contract change for callers.
- **`binary_blob`** — strategy is `binary_blob`; `ShellToolAdapter.parse_output` short-circuits before dispatch and the evidence pipeline owns the artifact.  No FindingDTO is emitted from the parser layer (by design).

### Catalog totals

| Status | Count | Share |
| --- | ---: | ---: |
| **`mapped`** | 118 | 75.16% |
| **`heartbeat`** | 39 | 24.84% |
| **`binary_blob`** | 0 | 0.00% |
| **Total** | **157** | **100.00%** |

### Parser coverage by category

Per :class:`ToolCategory` breakdown (ARG-030).  The `coverage` column is `mapped / total` as a percentage — a low value flags a category that lags the overall mapped ratio and is a strong candidate for the next parser batch.

| Category | mapped | heartbeat | binary_blob | total | coverage |
| --- | ---: | ---: | ---: | ---: | ---: |
| `recon` | 24 | 11 | 0 | 35 | 68.57% |
| `web_va` | 35 | 13 | 0 | 48 | 72.92% |
| `cloud` | 8 | 3 | 0 | 11 | 72.73% |
| `iac` | 4 | 0 | 0 | 4 | 100.00% |
| `network` | 19 | 4 | 0 | 23 | 82.61% |
| `auth` | 9 | 2 | 0 | 11 | 81.82% |
| `binary` | 5 | 0 | 0 | 5 | 100.00% |
| `browser` | 4 | 2 | 0 | 6 | 66.67% |
| `oast` | 2 | 2 | 0 | 4 | 50.00% |
| `misc` | 8 | 2 | 0 | 10 | 80.00% |
| **Total** | **118** | **39** | **0** | **157** | **75.16%** |

### Per-phase breakdown

| Phase | mapped | heartbeat | binary_blob | total |
| --- | ---: | ---: | ---: | ---: |
| `recon` | 39 | 17 | 0 | 56 |
| `vuln_analysis` | 62 | 17 | 0 | 79 |
| `exploitation` | 11 | 3 | 0 | 14 |
| `post_exploitation` | 6 | 2 | 0 | 8 |
| **Total** | **118** | **39** | **0** | **157** |

## Related modules

- `backend/src/sandbox/adapter_base.py`
- `backend/src/sandbox/tool_registry.py`
- `backend/src/sandbox/templating.py`
- `backend/src/sandbox/signing.py`
- `backend/src/sandbox/network_policies.py`
- `backend/src/sandbox/manifest.py`
- `backend/src/sandbox/k8s_adapter.py`
- `backend/src/sandbox/runner.py`
- `backend/scripts/tools_list.py`
- `backend/scripts/tools_sign.py`
- `backend/scripts/docs_tool_catalog.py`
- `backend/config/tools/SIGNATURES`
