apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "argus.fullname" . }}-grafana-quick
  labels:
    {{- include "argus.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  quick-mode.json: |-
{{ .Files.Get "dashboards/quick-mode.json" | indent 4 }}
