{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(argus_quick_scan_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95 duration",
          "refId": "A"
        }
      ],
      "title": "Quick scan duration p95",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(argus_quick_deadline_overrun_total[5m]))",
          "legendFormat": "deadline overrun",
          "refId": "A"
        }
      ],
      "title": "Quick deadline overrun",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "targets": [
        {
          "expr": "sum(rate(argus_quick_tasks_total[5m])) by (stage, status)",
          "legendFormat": "{{stage}} {{status}}",
          "refId": "A"
        }
      ],
      "title": "Quick tasks",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 4,
      "targets": [
        {
          "expr": "sum(rate(argus_quick_llm_calls_total[5m])) by (model, status)",
          "legendFormat": "{{model}} {{status}}",
          "refId": "A"
        }
      ],
      "title": "Quick LLM calls",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 5,
      "targets": [
        {
          "expr": "sum(rate(argus_quick_tool_failures_total[5m])) by (tool, reason)",
          "legendFormat": "{{tool}} {{reason}}",
          "refId": "A"
        }
      ],
      "title": "Quick tool failures",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 38,
  "tags": ["argus", "quick"],
  "title": "ARGUS Quick Mode",
  "uid": "argus-quick-mode",
  "version": 1
}
