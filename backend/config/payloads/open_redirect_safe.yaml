family_id: open_redirect_safe
description: >-
  Relative-only redirect curriculum markers (no protocol-relative URLs).
cwe_ids: [601]
owasp_top10:
  - 'A01:2021'
risk_level: low
requires_approval: false
oast_required: false
payloads:
  - id: relative_path
    template: "/argus-open-redirect-safe/static-curriculum"
    confidence: suspected
    notes: Same-origin relative path fragment only.
  - id: hash_only
    template: "#argus-open-redirect-safe-anchor"
    confidence: suspected
    notes: Document fragment only.
  - id: relative_query
    template: "/argus-open-redirect-safe/next?to=/local-only"
    confidence: suspected
    notes: Same-origin relative path with a local-only query parameter.
mutations:
  - name: case_flip
    description: Case-flip the path marker to exercise case-insensitive detection rules.
encodings:
  - name: identity
    stages: []
    description: Literal marker pass-through.
