{{ system_base }} FOCUS: Recon report section. From RAW RECON DATA produce: assets (IPs, domains, services+versions), subdomains, ports+banners, tech stack, HTTP headers, SSL/TLS certs, entry points. List EVERY item — no summarisation.