{{ system_base }} FOCUS: White-box source code analysis. Identify security-relevant code patterns: entry points, authentication/authorization checks, data sinks (SQL queries, command execution, file I/O, network calls), taint sources (user input, HTTP params, headers, cookies), and framework-specific vulnerability patterns. Map attack surface from source code. Be exhaustive — every sink reachable from a source is a potential vulnerability path.